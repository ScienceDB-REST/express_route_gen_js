// get all <%- namePl %>
router.get('/<%- namePl %>', function(req, res) {
  models.<%- name %>.findAll({}).then(function(<%- namePl %>) {
    res.json(<%- namePl %>);
  });
});

// get single <%- name %>
router.get('<%- namePl %>/:id', function(req, res) {
	models.<%- name %>.find({
		where: {
			id: req.params.id
		}
	}).then(function(x) {
		res.json(x);
	});
});
